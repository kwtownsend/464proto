from django.conf.urls import patterns, include, url
from . import views


urlpatterns = [
    # url(
 #        regex=r"^(?P<pk>\d+)$",
 #        view=views.LocationDetailView.as_view(),
 #        name="detail"
 #        ),
    ]
